<div class="row calls">
  <div class="col-md-12">

    <div class="row">
      <div class="col-md-12">
        <h2>Calls</h2>
      </div>
    </div>

    <div class="row">
      <div class="col-md-12">
        <table class="table">
          <thead>
            <th>Date</th>
            <th>From</th>
            <th>To</th>
            <th>Disposition</th>
            <th>Recording</th>
          </thead>
          <% lead.calls.each do |call| %>
            <tr>
              <td><%= long_datetime(DateTime.parse(call["date"])) rescue '' %></td>
              <td><%= call["src"] %></td>
              <td><%= call["dst"] %></td>
              <td><%= call["disposition"] %></td>
              <td><%= call["recordingfile"].empty? ? 'None' : call["recordingfile"] %></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>

