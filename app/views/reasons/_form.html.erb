<%= form_with(model: reason, local: true) do |form| %>
  <% if reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reason.errors.count, "error") %> prohibited this reason from being saved:</h2>

      <ul>
      <% reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="fieldset_content">
    <div class="row">
      <div class="form-group">
        <%= form.label :name %>
        <%= form.text_field :name, id: :reason_name, class: "form-control" %>
      </div>
    </div>

    <div class="row">
      <div class="form-group">
        <%= form.label :description %>
        <%= form.text_field :description, id: :reason_description, class: "form-control" %>
      </div>
    </div>

    <div class="form-check-label">
      <%= form.check_box :active, class: 'form-check-input' %>
      Reason is active?
    </div>
  </div>

  <div class="form-group">
    <button type="submit" class="btn btn-primary">Submit</button>
  </div>
<% end %>
