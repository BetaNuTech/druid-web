<%= form_with(model: lead_action, local: true) do |form| %>
  <% if lead_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lead_action.errors.count, "error") %> prohibited this lead_action from being saved:</h2>

      <ul>
      <% lead_action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id, id: :lead_action_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :lead_action_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :lead_action_description %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :lead_action_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
