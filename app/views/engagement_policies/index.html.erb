<p id="notice"><%= notice %></p>

<h1>Engagement Policies</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @engagement_policies.each do |engagement_policy| %>
      <tr>
        <td><%= link_to 'Show', engagement_policy %></td>
        <td><%= link_to 'Edit', edit_engagement_policy_path(engagement_policy) %></td>
        <td><%= link_to 'Destroy', engagement_policy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Engagement Policy', new_engagement_policy_path %>
