<%- if @message.present? %>
  $('#html_editor_v2').val('<%= raw escape_javascript(@message) %>');
  if (window.html_editor_v2 != undefined) {
    window.html_editor_v2.html.set('<%= raw escape_javascript(@message) %>');
  }
<%- end %>
