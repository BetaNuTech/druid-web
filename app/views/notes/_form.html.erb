<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :note_user_id %>
  </div>

  <div class="field">
    <%= form.label :lead_action_id %>
    <%= form.text_field :lead_action_id, id: :note_lead_action_id %>
  </div>

  <div class="field">
    <%= form.label :reason_id %>
    <%= form.text_field :reason_id, id: :note_reason_id %>
  </div>

  <div class="field">
    <%= form.label :notable_id %>
    <%= form.text_field :notable_id, id: :note_notable_id %>
  </div>

  <div class="field">
    <%= form.label :notable_type %>
    <%= form.text_field :notable_type, id: :note_notable_type %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :note_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
